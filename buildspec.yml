version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12.x
  pre_build:
    commands:
      - node -v
      - npm -v
  build:
    commands:
      - echo Build started on `date`
       
  post_build:
    commands:
      - echo Build completed on `date`
      
                 
#artifacts:
#  files:
#    - dist
